diff --git a/node_modules/expo-audio/.bun-tag-199f56dd6f7f9643 b/.bun-tag-199f56dd6f7f9643
new file mode 100644
index 0000000000000000000000000000000000000000..e69de29bb2d1d6434b8b29ae775ad8c2e48c5391
diff --git a/android/src/main/java/expo/modules/audio/AudioModule.kt b/android/src/main/java/expo/modules/audio/AudioModule.kt
index 992239ba7631ce0c97c0b41c5fbf61cc842386fe..f31b6c1acc0075cf98c9eb94c1d2f4a7587f29be 100644
--- a/android/src/main/java/expo/modules/audio/AudioModule.kt
+++ b/android/src/main/java/expo/modules/audio/AudioModule.kt
@@ -49,7 +49,19 @@ class AudioModule : Module() {
   private lateinit var audioManager: AudioManager
   private val context: Context
     get() = appContext.reactContext ?: throw Exceptions.ReactContextLost()
-  private val httpClient = OkHttpClient()
+  // Route audio streaming through React Native's shared OkHttpClient, which the
+  // ssl-trust module patches with the app's custom SSLSocketFactory (self-signed
+  // TOFU trust + mTLS client cert). A bare OkHttpClient() uses the system default
+  // trust manager, so it can't reach self-signed / mutual-TLS servers. Reflection
+  // avoids a compile-time dependency on react-android; falls back to a plain
+  // client if RN's provider can't be reached.
+  private val httpClient: OkHttpClient = try {
+    Class.forName("com.facebook.react.modules.network.OkHttpClientProvider")
+      .getMethod("getOkHttpClient")
+      .invoke(null) as OkHttpClient
+  } catch (e: Exception) {
+    OkHttpClient()
+  }
 
   private val players = ConcurrentHashMap<String, AudioPlayer>()
   private val recorders = ConcurrentHashMap<String, AudioRecorder>()
@@ -148,11 +160,11 @@ class AudioModule : Module() {
     val result = if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
       val requestType = interruptionMode?.let {
         if (it == InterruptionMode.DO_NOT_MIX) {
-          AudioManager.AUDIOFOCUS_GAIN_TRANSIENT
+          AudioManager.AUDIOFOCUS_GAIN
         } else {
           AudioManager.AUDIOFOCUS_GAIN_TRANSIENT_MAY_DUCK
         }
-      } ?: AudioManager.AUDIOFOCUS_GAIN_TRANSIENT
+      } ?: AudioManager.AUDIOFOCUS_GAIN
       audioFocusRequest = AudioFocusRequest.Builder(requestType).run {
         setAudioAttributes(
           AudioAttributes.Builder()
@@ -169,7 +181,7 @@ class AudioModule : Module() {
     } else {
       @Suppress("DEPRECATION")
       val requestType = if (interruptionMode == InterruptionMode.DO_NOT_MIX) {
-        AudioManager.AUDIOFOCUS_GAIN_TRANSIENT
+        AudioManager.AUDIOFOCUS_GAIN
       } else {
         AudioManager.AUDIOFOCUS_GAIN_TRANSIENT_MAY_DUCK
       }
@@ -464,6 +476,12 @@ class AudioModule : Module() {
         ref.setVolume(volume)
       }
 
+      Property("audioSessionId") { player ->
+        runOnMain {
+          player.ref.audioSessionId
+        }
+      }
+
       Function("play") { player: AudioPlayer ->
         if (!audioEnabled) {
           Log.e(TAG, "Audio has been disabled. Re-enable to start playing")
@@ -525,6 +543,16 @@ class AudioModule : Module() {
         }
       }
 
+      Function("setRemotePlayback") { ref: AudioPlayer, active: Boolean ->
+        runOnMain {
+          ref.setRemotePlayback(active)
+        }
+      }
+
+      Function("updateRemotePlayback") { ref: AudioPlayer, playing: Boolean, positionMs: Double, durationMs: Double ->
+        ref.updateRemotePlayback(playing, positionMs.toLong(), durationMs.toLong())
+      }
+
       Function("setAudioSamplingEnabled") { player: AudioPlayer, enabled: Boolean ->
         runOnMain {
           player.setSamplingEnabled(enabled)
diff --git a/android/src/main/java/expo/modules/audio/AudioPlayer.kt b/android/src/main/java/expo/modules/audio/AudioPlayer.kt
index c76a9fd2a53743a691782e0f67d0d71401df58f6..e815f13a9d9bb5e3b5e9b2acaf000a1cc46ec8c4 100644
--- a/android/src/main/java/expo/modules/audio/AudioPlayer.kt
+++ b/android/src/main/java/expo/modules/audio/AudioPlayer.kt
@@ -24,6 +24,11 @@ import java.lang.ref.WeakReference
 
 private const val PLAYBACK_STATUS_UPDATE = "playbackStatusUpdate"
 private const val AUDIO_SAMPLE_UPDATE = "audioSampleUpdate"
+private const val REMOTE_PREVIOUS = "remotePrevious"
+private const val REMOTE_NEXT = "remoteNext"
+private const val REMOTE_PLAY = "remotePlay"
+private const val REMOTE_PAUSE = "remotePause"
+private const val REMOTE_SEEK = "remoteSeek"
 private const val SEEK_JUMP_INTERVAL_MS: Long = 10_000
 
 @UnstableApi
@@ -37,6 +42,8 @@ class AudioPlayer(
   player = ExoPlayer.Builder(context)
     .setLooper(context.mainLooper)
     .setAudioAttributes(AudioAttributes.DEFAULT, false)
+    .setHandleAudioBecomingNoisy(true)
+    .setWakeMode(C.WAKE_MODE_NETWORK)
     .setSeekForwardIncrementMs(SEEK_JUMP_INTERVAL_MS)
     .setSeekBackIncrementMs(SEEK_JUMP_INTERVAL_MS)
     .apply {
@@ -97,6 +104,38 @@ class AudioPlayer(
     startUpdating()
   }
 
+  fun emitRemotePrevious() {
+    emit(REMOTE_PREVIOUS)
+  }
+
+  fun emitRemoteNext() {
+    emit(REMOTE_NEXT)
+  }
+
+  // Only fired while a remote device owns playback; locally the media controls
+  // act on this player directly and never take the JS round trip.
+  fun emitRemotePlay() {
+    emit(REMOTE_PLAY)
+  }
+
+  fun emitRemotePause() {
+    emit(REMOTE_PAUSE)
+  }
+
+  fun emitRemoteSeek(positionMs: Long) {
+    emit(REMOTE_SEEK, positionMs.toDouble())
+  }
+
+  fun setRemotePlayback(active: Boolean) {
+    serviceConnection.playbackServiceBinder?.service?.setRemotePlaybackActive(active)
+  }
+
+  fun updateRemotePlayback(playing: Boolean, positionMs: Long, durationMs: Long) {
+    serviceConnection.playbackServiceBinder
+      ?.service
+      ?.updateRemotePlaybackState(playing, positionMs, durationMs)
+  }
+
   fun setActiveForLockScreen(active: Boolean, metadata: Metadata? = null, options: AudioLockScreenOptions? = null) {
     if (active) {
       this.metadata = metadata
diff --git a/android/src/main/java/expo/modules/audio/AudioPlaylist.kt b/android/src/main/java/expo/modules/audio/AudioPlaylist.kt
index f3536d571535db7d6af57f568fce0d7c0b3fb674..b62725b5f2a09b0c1c7ac7b1de0517dd36a35969 100644
--- a/android/src/main/java/expo/modules/audio/AudioPlaylist.kt
+++ b/android/src/main/java/expo/modules/audio/AudioPlaylist.kt
@@ -2,6 +2,7 @@ package expo.modules.audio
 
 import android.content.Context
 import androidx.media3.common.AudioAttributes
+import androidx.media3.common.C
 import androidx.media3.common.MediaItem
 import androidx.media3.common.PlaybackException
 import androidx.media3.common.Player
@@ -25,6 +26,8 @@ class AudioPlaylist(
   player = ExoPlayer.Builder(context)
     .setLooper(context.mainLooper)
     .setAudioAttributes(AudioAttributes.DEFAULT, false)
+    .setHandleAudioBecomingNoisy(true)
+    .setWakeMode(C.WAKE_MODE_NETWORK)
     .setMediaSourceFactory(DefaultMediaSourceFactory(context).setDataSourceFactory(dataSourceFactory))
     .build(),
   appContext = appContext,
diff --git a/android/src/main/java/expo/modules/audio/AudioRecords.kt b/android/src/main/java/expo/modules/audio/AudioRecords.kt
index 86880d92fd5b6413e8596e1b2b8ae28f2981addd..771404a6488ac9bd2607f62a51687381d5095dc8 100644
--- a/android/src/main/java/expo/modules/audio/AudioRecords.kt
+++ b/android/src/main/java/expo/modules/audio/AudioRecords.kt
@@ -55,7 +55,8 @@ class Metadata(
   @Field val title: String?,
   @Field val artist: String?,
   @Field val albumTitle: String?,
-  @Field val artworkUrl: URL?
+  @Field val artworkUrl: URL?,
+  @Field val durationMs: Double? = null
 ) : Record
 
 enum class AndroidOutputFormat(val value: String) : Enumerable {
@@ -110,7 +111,9 @@ enum class AndroidAudioEncoder(val value: String) : Enumerable {
 class AudioLockScreenOptions(
   @Field val showSeekForward: Boolean,
   @Field val showSeekBackward: Boolean,
-  @Field val isLiveStream: Boolean? = null
+  @Field val isLiveStream: Boolean? = null,
+  @Field val showSkipPrevious: Boolean = false,
+  @Field val showSkipNext: Boolean = false
 ) : Record
 
 enum class InterruptionMode(val value: String) : Enumerable {
diff --git a/android/src/main/java/expo/modules/audio/service/AudioControlsService.kt b/android/src/main/java/expo/modules/audio/service/AudioControlsService.kt
index 196a5d57f887c2667a88543bd2f6a80044a044f2..eeaad30b6ad88137c210092350f656f15f128d31 100644
--- a/android/src/main/java/expo/modules/audio/service/AudioControlsService.kt
+++ b/android/src/main/java/expo/modules/audio/service/AudioControlsService.kt
@@ -6,36 +6,44 @@ import android.app.NotificationManager
 import android.app.PendingIntent
 import android.content.Context
 import android.content.Intent
-import android.media.AudioManager
 import android.content.pm.ServiceInfo
 import android.graphics.Bitmap
 import android.graphics.BitmapFactory
+import android.media.AudioManager
 import android.os.Build
 import android.os.Bundle
+import android.os.Handler
 import android.os.IBinder
+import android.os.Looper
 import androidx.annotation.OptIn
 import androidx.core.app.NotificationCompat
+import androidx.media3.common.C
 import androidx.media3.common.ForwardingPlayer
+import androidx.media3.common.MediaItem
 import androidx.media3.common.Player
+import androidx.media3.common.SimpleBasePlayer
 import androidx.media3.common.util.UnstableApi
 import androidx.media3.session.CommandButton
 import androidx.media3.session.MediaSession
 import androidx.media3.session.MediaSessionService
 import androidx.media3.session.MediaStyleNotificationHelper
 import androidx.media3.session.SessionCommand
+import com.google.common.collect.ImmutableList
+import com.google.common.util.concurrent.Futures
+import com.google.common.util.concurrent.ListenableFuture
 import expo.modules.audio.AudioLockScreenOptions
 import expo.modules.audio.AudioPlayer
 import expo.modules.audio.Metadata
 import expo.modules.audio.getPlaybackServiceErrorMessage
 import expo.modules.kotlin.AppContext
+import java.lang.ref.WeakReference
+import java.net.URL
 import kotlinx.coroutines.CoroutineScope
 import kotlinx.coroutines.Dispatchers
 import kotlinx.coroutines.Job
 import kotlinx.coroutines.cancel
 import kotlinx.coroutines.isActive
 import kotlinx.coroutines.launch
-import java.lang.ref.WeakReference
-import java.net.URL
 
 @OptIn(UnstableApi::class)
 class AudioControlsService : MediaSessionService() {
@@ -43,6 +51,12 @@ class AudioControlsService : MediaSessionService() {
   private val binder = AudioPlaybackServiceBinder(this)
   private var mediaSession: MediaSession? = null
   private var sessionMetadataPlayer: MetadataInjectingPlayer? = null
+  // Whether JS says playback belongs to a remote device rather than this one's
+  // audio engine, and the player standing in for it. Kept apart because
+  // setPlayerOptions rebuilds the session from the ExoPlayer, so the swap has to
+  // be re-applied afterwards rather than lost. See RemotePlaybackPlayer.
+  @Volatile private var remotePlaybackActive = false
+  @Volatile private var remotePlayer: RemotePlaybackPlayer? = null
   private var currentMetadata: Metadata? = null
   private var currentPlayer: AudioPlayer? = null
   private var currentOptions: AudioLockScreenOptions? = null
@@ -73,23 +87,30 @@ class AudioControlsService : MediaSessionService() {
       return super.onStartCommand(intent, flags, startId)
     }
 
+    // While a remote device owns playback the ExoPlayer holds nothing, so these
+    // buttons have to act on the session's player instead — otherwise play
+    // starts this device's audio on top of the remote. Falls back to the
+    // ExoPlayer, which is what backs the session for local playback.
+    val transportRef: Player = remotePlayer ?: currentPlayerRef
     context.mainQueue.launch {
       when (intent?.action) {
         ACTION_PLAY -> {
           if (shouldPlayInSilentMode()) {
-            currentPlayerRef.play()
+            transportRef.play()
           }
         }
-        ACTION_PAUSE -> currentPlayerRef.pause()
+        ACTION_PAUSE -> transportRef.pause()
         ACTION_TOGGLE ->
-          if (currentPlayerRef.isPlaying) {
-            currentPlayerRef.pause()
+          if (transportRef.isPlaying) {
+            transportRef.pause()
           } else if (shouldPlayInSilentMode()) {
-            currentPlayerRef.play()
+            transportRef.play()
           }
 
-        ACTION_SEEK_FORWARD -> currentPlayerRef.seekTo(currentPlayerRef.currentPosition + SEEK_INTERVAL_MS)
-        ACTION_SEEK_BACKWARD -> currentPlayerRef.seekTo(currentPlayerRef.currentPosition - SEEK_INTERVAL_MS)
+        ACTION_SEEK_FORWARD -> transportRef.seekTo(transportRef.currentPosition + SEEK_INTERVAL_MS)
+        ACTION_SEEK_BACKWARD -> transportRef.seekTo(transportRef.currentPosition - SEEK_INTERVAL_MS)
+        ACTION_SKIP_PREVIOUS -> currentPlayer?.emitRemotePrevious()
+        ACTION_SKIP_NEXT -> currentPlayer?.emitRemoteNext()
       }
     }
 
@@ -192,6 +213,18 @@ class AudioControlsService : MediaSessionService() {
       val compactViewIndices = mutableListOf<Int>()
       var currentIndex = 0
 
+      if (currentOptions?.showSkipPrevious == true) {
+        builder.addAction(
+          NotificationCompat.Action(
+            androidx.media3.session.R.drawable.media3_icon_previous,
+            "Skip Previous",
+            buildActionPendingIntent(ACTION_SKIP_PREVIOUS)
+          )
+        )
+        compactViewIndices.add(currentIndex)
+        currentIndex++
+      }
+
       if (currentOptions?.showSeekBackward == true) {
         builder.addAction(
           NotificationCompat.Action(
@@ -227,6 +260,18 @@ class AudioControlsService : MediaSessionService() {
           )
         )
         compactViewIndices.add(currentIndex)
+        currentIndex++
+      }
+
+      if (currentOptions?.showSkipNext == true) {
+        builder.addAction(
+          NotificationCompat.Action(
+            androidx.media3.session.R.drawable.media3_icon_next,
+            "Skip Next",
+            buildActionPendingIntent(ACTION_SKIP_NEXT)
+          )
+        )
+        compactViewIndices.add(currentIndex)
       }
 
       style.setShowActionsInCompactView(*compactViewIndices.toIntArray())
@@ -240,14 +285,38 @@ class AudioControlsService : MediaSessionService() {
     val session = mediaSession ?: return
     val mediaButtons = mutableListOf<CommandButton>()
 
+    val showSkipPrevious = currentOptions?.showSkipPrevious == true
+    val showSkipNext = currentOptions?.showSkipNext == true
+    val showSeekBackward = currentOptions?.showSeekBackward == true
+    val showSeekForward = currentOptions?.showSeekForward == true
+
+    // No CommandButton is declared for skip previous/next, and must not be.
+    // Media3 strips ACTION_SKIP_TO_PREVIOUS / ACTION_SKIP_TO_NEXT from the legacy
+    // PlaybackState whenever a media button preference occupies SLOT_BACK /
+    // SLOT_FORWARD, on the assumption that legacy controllers should invoke the
+    // custom action instead. Voice assistants and the Bluetooth AVRCP bridge only
+    // understand the standard actions, so declaring a button there makes the app
+    // look like a player that cannot change track at all. Leaving both slots free
+    // keeps the standard actions advertised, and the system media controls fill
+    // them with the usual previous/next buttons because COMMAND_SEEK_TO_PREVIOUS /
+    // COMMAND_SEEK_TO_NEXT are available on the forwarding player below — which
+    // routes them to the JS-managed queue, same as the removed buttons did.
+    // showSkipPrevious / showSkipNext still gate that capability; they only stop
+    // driving the button layout.
+
     // Add seek backward button if enabled
-    if (currentOptions?.showSeekBackward == true) {
+    if (showSeekBackward) {
+      val slot = if (showSkipPrevious) {
+        intArrayOf(CommandButton.SLOT_BACK_SECONDARY, CommandButton.SLOT_OVERFLOW)
+      } else {
+        intArrayOf(CommandButton.SLOT_BACK, CommandButton.SLOT_OVERFLOW)
+      }
       mediaButtons.add(
         CommandButton.Builder(CommandButton.ICON_SKIP_BACK_10)
           .setDisplayName("Seek Backward")
           .setEnabled(true)
           .setSessionCommand(SessionCommand(ACTION_SEEK_BACKWARD, Bundle.EMPTY))
-          .setSlots(CommandButton.SLOT_BACK)
+          .setSlots(*slot)
           .build()
       )
     }
@@ -263,17 +332,25 @@ class AudioControlsService : MediaSessionService() {
     )
 
     // Add seek forward button if enabled
-    if (currentOptions?.showSeekForward == true) {
+    if (showSeekForward) {
+      val slot = if (showSkipNext) {
+        intArrayOf(CommandButton.SLOT_FORWARD_SECONDARY, CommandButton.SLOT_OVERFLOW)
+      } else {
+        intArrayOf(CommandButton.SLOT_FORWARD, CommandButton.SLOT_OVERFLOW)
+      }
       mediaButtons.add(
         CommandButton.Builder(CommandButton.ICON_SKIP_FORWARD_10)
           .setDisplayName("Seek Forward")
           .setEnabled(true)
           .setSessionCommand(SessionCommand(ACTION_SEEK_FORWARD, Bundle.EMPTY))
-          .setSlots(CommandButton.SLOT_FORWARD)
+          .setSlots(*slot)
           .build()
       )
     }
 
+    // Skip next is deliberately absent from the forward slot; see the note above
+    // the seek backward button.
+
     session.setCustomLayout(mediaButtons)
     session.setMediaButtonPreferences(mediaButtons)
   }
@@ -321,16 +398,76 @@ class AudioControlsService : MediaSessionService() {
   }
 
   private fun resolveSessionPlayer(player: AudioPlayer, options: AudioLockScreenOptions?): Player {
-    val isLive = options?.isLiveStream ?: player.isLive
-    if (!isLive) {
-      return player.ref
-    }
+    return wrapForQueueNavigation(
+      inner = player.ref,
+      emitter = player,
+      isLive = options?.isLiveStream ?: player.isLive,
+      options = options
+    )
+  }
 
-    return object : ForwardingPlayer(player.ref) {
+  // Whatever backs the session — the ExoPlayer for local playback, a
+  // RemotePlaybackPlayer for a jukebox or renderer — owns a single item while the
+  // JS layer owns the queue, so both need the same skip re-routing.
+  private fun wrapForQueueNavigation(
+    inner: Player,
+    emitter: AudioPlayer,
+    isLive: Boolean,
+    options: AudioLockScreenOptions?
+  ): Player {
+    val showSkipPrevious = options?.showSkipPrevious == true
+    val showSkipNext = options?.showSkipNext == true
+
+    // The JS layer owns the queue and only ever loads a single MediaItem into the
+    // ExoPlayer, so ExoPlayer reports no next/previous item and the framework would
+    // drop hardware/Bluetooth skip presses. Advertise the navigation commands here
+    // and re-route the resulting seekToNext/Previous player calls (which is what
+    // Media3 maps KEYCODE_MEDIA_NEXT/PREVIOUS onto) back to the JS queue.
+    return object : ForwardingPlayer(inner) {
       override fun getAvailableCommands(): Player.Commands {
-        return super.getAvailableCommands().buildUpon()
-          .remove(Player.COMMAND_SEEK_IN_CURRENT_MEDIA_ITEM)
-          .build()
+        val builder = super.getAvailableCommands().buildUpon()
+        if (isLive) {
+          builder.remove(Player.COMMAND_SEEK_IN_CURRENT_MEDIA_ITEM)
+        }
+        // Android's AVRCP bridge (MediaPlayerWrapper.isMetadataSynced) compares the
+        // legacy queue's active item against the session metadata and defers every
+        // update to the Bluetooth device by 2s while the two disagree. The single
+        // MediaItem loaded here is a bare stream URI with no title/artist, while the
+        // metadata is injected separately, so they can never match. Hiding the
+        // timeline makes Media3 publish an empty legacy queue and an UNKNOWN active
+        // queue id — the case AVRCP treats as synced — so updates go out at once.
+        builder.remove(Player.COMMAND_GET_TIMELINE)
+        if (showSkipPrevious) {
+          builder.add(Player.COMMAND_SEEK_TO_PREVIOUS)
+          builder.add(Player.COMMAND_SEEK_TO_PREVIOUS_MEDIA_ITEM)
+        }
+        if (showSkipNext) {
+          builder.add(Player.COMMAND_SEEK_TO_NEXT)
+          builder.add(Player.COMMAND_SEEK_TO_NEXT_MEDIA_ITEM)
+        }
+        return builder.build()
+      }
+
+      override fun hasPreviousMediaItem(): Boolean =
+        if (showSkipPrevious) true else super.hasPreviousMediaItem()
+
+      override fun hasNextMediaItem(): Boolean =
+        if (showSkipNext) true else super.hasNextMediaItem()
+
+      override fun seekToPrevious() {
+        if (showSkipPrevious) emitter.emitRemotePrevious() else super.seekToPrevious()
+      }
+
+      override fun seekToPreviousMediaItem() {
+        if (showSkipPrevious) emitter.emitRemotePrevious() else super.seekToPreviousMediaItem()
+      }
+
+      override fun seekToNext() {
+        if (showSkipNext) emitter.emitRemoteNext() else super.seekToNext()
+      }
+
+      override fun seekToNextMediaItem() {
+        if (showSkipNext) emitter.emitRemoteNext() else super.seekToNextMediaItem()
       }
     }
   }
@@ -364,6 +501,7 @@ class AudioControlsService : MediaSessionService() {
     if (player != null) {
       mediaSession?.release()
       sessionMetadataPlayer = null
+      remotePlayer = null
 
       appContext?.mainQueue?.launch {
         val context = appContext?.reactContext ?: return@launch
@@ -371,7 +509,12 @@ class AudioControlsService : MediaSessionService() {
           updateMetadata(metadata)
         }
         val session = MediaSession.Builder(context, sessionPlayer)
-          .setCallback(AudioMediaSessionCallback())
+          .setCallback(
+            AudioMediaSessionCallback(
+              onSkipPrevious = { currentPlayer?.emitRemotePrevious() },
+              onSkipNext = { currentPlayer?.emitRemoteNext() }
+            )
+          )
           .build()
 
         // Replace the basic media session with a session connected to our playback service.
@@ -388,6 +531,8 @@ class AudioControlsService : MediaSessionService() {
 
         addPlayerListener(player)
 
+        applyRemotePlaybackToSession()
+
         // Initial update now that session exists
         postOrStartForegroundNotification(startInForeground = false)
       }
@@ -412,6 +557,8 @@ class AudioControlsService : MediaSessionService() {
 
   private fun clearSessionInternal() {
     currentPlayer?.isActiveForLockScreen = false
+    remotePlaybackActive = false
+    remotePlayer = null
     removePlayerListener()
     currentPlayer = null
     currentMetadata = null
@@ -446,13 +593,19 @@ class AudioControlsService : MediaSessionService() {
 
       mediaSession?.release()
       sessionMetadataPlayer = null
+      remotePlayer = null
       appContext?.mainQueue?.launch {
         val context = appContext?.reactContext ?: return@launch
         val sessionPlayer = MetadataInjectingPlayer(resolveSessionPlayer(player, options)).apply {
           updateMetadata(metadata)
         }
         val session = MediaSession.Builder(context, sessionPlayer)
-          .setCallback(AudioMediaSessionCallback())
+          .setCallback(
+            AudioMediaSessionCallback(
+              onSkipPrevious = { currentPlayer?.emitRemotePrevious() },
+              onSkipNext = { currentPlayer?.emitRemoteNext() }
+            )
+          )
           .build()
 
         player.mediaSession.release()
@@ -462,7 +615,9 @@ class AudioControlsService : MediaSessionService() {
         mediaSession = session
         sessionMetadataPlayer = sessionPlayer
 
-        updateSessionCustomLayout(player.ref.isPlaying)
+        applyRemotePlaybackToSession()
+
+        updateSessionCustomLayout(session.player.isPlaying)
         postOrStartForegroundNotification(startInForeground = false)
       }
 
@@ -482,6 +637,75 @@ class AudioControlsService : MediaSessionService() {
     clearSessionInternal()
   }
 
+  /**
+   * Hand the session over to (or take it back from) a remote device — a Subsonic
+   * jukebox, a UPnP renderer. Swapping the session's player rather than
+   * rebuilding the session keeps the notification in place across the handover,
+   * along with the injected metadata, the artwork and the foreground promotion
+   * the local session already owns.
+   */
+  fun setRemotePlaybackActive(active: Boolean) {
+    if (remotePlaybackActive == active) return
+    remotePlaybackActive = active
+    appContext?.mainQueue?.launch { applyRemotePlaybackToSession() }
+  }
+
+  fun updateRemotePlaybackState(playing: Boolean, positionMs: Long, durationMs: Long) {
+    // Queued rather than applied inline: setRemotePlaybackActive posts the swap
+    // to the same queue, so a push issued right after it would otherwise arrive
+    // before there is a player to receive it.
+    appContext?.mainQueue?.launch {
+      val remote = remotePlayer ?: return@launch
+      val controlsChanged = remote.controlsWouldChange(playing, positionMs, durationMs)
+      remote.applyState(playing, positionMs, durationMs)
+      // addPlayerListener is attached to the ExoPlayer, which no longer moves
+      // while the remote owns playback, so nothing else refreshes the controls.
+      // Position rides on the session's PlaybackState though, so a position-only
+      // push needs no rebuild — otherwise the caller's 1s pulse would re-post the
+      // foreground notification every second for the whole session.
+      if (controlsChanged) {
+        updateSessionCustomLayout(playing)
+        postOrStartForegroundNotification(startInForeground = false)
+      }
+    }
+  }
+
+  // Bring the session's player in line with remotePlaybackActive. Idempotent, so
+  // it can be re-run after any session rebuild. Main thread only.
+  private fun applyRemotePlaybackToSession() {
+    val session = mediaSession ?: return
+    val player = currentPlayer ?: return
+    if (remotePlaybackActive) {
+      if (remotePlayer != null) return
+      val remote = RemotePlaybackPlayer(
+        onPlay = { player.emitRemotePlay() },
+        onPause = { player.emitRemotePause() },
+        onSeek = { positionMs -> player.emitRemoteSeek(positionMs) }
+      )
+      remotePlayer = remote
+      installSessionPlayer(
+        session,
+        wrapForQueueNavigation(remote, player, isLive = false, options = currentOptions)
+      )
+    } else {
+      if (remotePlayer == null) return
+      remotePlayer = null
+      installSessionPlayer(session, resolveSessionPlayer(player, currentOptions))
+    }
+    updateSessionCustomLayout(session.player.isPlaying)
+    postOrStartForegroundNotification(startInForeground = false)
+  }
+
+  // The outgoing player is dropped rather than released: locally it wraps the
+  // shared ExoPlayer, which the JS layer still owns.
+  private fun installSessionPlayer(session: MediaSession, player: Player) {
+    val sessionPlayer = MetadataInjectingPlayer(player).apply {
+      updateMetadata(currentMetadata)
+    }
+    session.player = sessionPlayer
+    sessionMetadataPlayer = sessionPlayer
+  }
+
   private fun loadArtworkFromUrl(url: URL, callback: (Bitmap?) -> Unit) {
     if (url != currentArtworkUrl) {
       currentArtworkUrl = url
@@ -516,6 +740,8 @@ class AudioControlsService : MediaSessionService() {
     artworkLoadJob?.cancel()
     artworkLoadJob = null
     scope.cancel()
+    remotePlaybackActive = false
+    remotePlayer = null
     mediaSession?.release()
     mediaSession = null
     sessionMetadataPlayer = null
@@ -558,7 +784,150 @@ class AudioControlsService : MediaSessionService() {
 
     const val ACTION_SEEK_FORWARD = "expo.modules.audio.action.SEEK_FORWARD"
     const val ACTION_SEEK_BACKWARD = "expo.modules.audio.action.SEEK_BACKWARD"
+    const val ACTION_SKIP_PREVIOUS = "expo.modules.audio.action.SKIP_PREVIOUS"
+    const val ACTION_SKIP_NEXT = "expo.modules.audio.action.SKIP_NEXT"
 
     const val SEEK_INTERVAL_MS = 10000L
   }
 }
+
+/**
+ * Media3 Player whose state is pushed from JS and whose transport commands are
+ * forwarded back to JS, for playback that happens somewhere other than this
+ * device's audio engine (a Subsonic jukebox, a UPnP renderer).
+ *
+ * The ExoPlayer holds nothing in that case, so a session built on it reports a
+ * stale track and a paused state, and its play button starts the phone's own
+ * audio on top of the remote. Swapping this in as the session's player while the
+ * remote owns playback makes the notification, the lock screen and every media
+ * controller reflect what is really playing.
+ *
+ * Metadata is not carried here: [MetadataInjectingPlayer] wraps this and keeps
+ * supplying whatever the JS layer last passed to `updateLockScreenMetadata`,
+ * exactly as it does for local playback. This only owns transport state.
+ */
+@OptIn(UnstableApi::class)
+internal class RemotePlaybackPlayer(
+  private val onPlay: () -> Unit,
+  private val onPause: () -> Unit,
+  private val onSeek: (positionMs: Long) -> Unit
+) : SimpleBasePlayer(Looper.getMainLooper()) {
+
+  @Volatile private var playing: Boolean = false
+  @Volatile private var positionMs: Long = 0L
+  @Volatile private var positionUpdatedAt: Long = System.currentTimeMillis()
+  @Volatile private var durationMs: Long = 0L
+  // Set for exactly one state update when a push moves the position by more than
+  // playback alone could explain. Media3 treats a same-item position change under
+  // its own 1s threshold as continuous progress and never republishes it, so the
+  // notification's seek bar would keep extrapolating from the value it last saw.
+  private var pendingDiscontinuityMs: Long = C.TIME_UNSET
+
+  private val mainHandler = Handler(Looper.getMainLooper())
+
+  // SimpleBasePlayer requires its application thread (main). JS calls land on
+  // the JS thread, so hop to the main looper before mutating + invalidating.
+  private fun runOnMain(block: () -> Unit) {
+    if (Looper.myLooper() == Looper.getMainLooper()) block() else mainHandler.post(block)
+  }
+
+  // Whether a push would move anything the notification or the custom layout is
+  // built from. A plain position tick would not — position travels in the
+  // session's PlaybackState — but a jump is worth the refresh.
+  fun controlsWouldChange(isPlaying: Boolean, posMs: Long, durMs: Long): Boolean =
+    playing != isPlaying || durationMs != durMs.coerceAtLeast(0L) || isJump(posMs)
+
+  private fun currentPositionMs(): Long = if (playing) {
+    positionMs + (System.currentTimeMillis() - positionUpdatedAt)
+  } else {
+    positionMs
+  }
+
+  // Matches media3's own POSITION_DISCONTINUITY_THRESHOLD_MS, so anything it
+  // would have downgraded to DISCONTINUITY_REASON_INTERNAL is raised as a seek
+  // instead, while the caller's 1s pulse stays quiet.
+  private fun isJump(posMs: Long): Boolean =
+    Math.abs(posMs.coerceAtLeast(0L) - currentPositionMs()) > POSITION_JUMP_TOLERANCE_MS
+
+  fun applyState(isPlaying: Boolean, posMs: Long, durMs: Long) = runOnMain {
+    if (isJump(posMs)) pendingDiscontinuityMs = posMs.coerceAtLeast(0L)
+    playing = isPlaying
+    positionMs = posMs.coerceAtLeast(0L)
+    positionUpdatedAt = System.currentTimeMillis()
+    durationMs = durMs.coerceAtLeast(0L)
+    invalidateState()
+    // invalidateState reads the state synchronously on this thread, so clearing
+    // here rather than inside getState keeps that pure — media3 also calls it
+    // outside an update (a controller connecting, a placeholder state).
+    pendingDiscontinuityMs = C.TIME_UNSET
+  }
+
+  override fun getState(): State {
+    // A remote reports its position every second at best, so advance it off the
+    // wall clock between pushes or the notification's seek bar would step.
+    val extrapolated = currentPositionMs()
+
+    // One placeholder item, carrying only the duration. The session needs a
+    // non-empty timeline — media3 drops transport commands inside BasePlayer on
+    // an empty one — but the item itself is never shown: MetadataInjectingPlayer
+    // supplies title/artist/artwork on top of it.
+    val item = MediaItemData.Builder(REMOTE_ITEM_UID)
+      .setMediaItem(MediaItem.Builder().setMediaId(REMOTE_ITEM_UID).build())
+      .setDurationUs(if (durationMs > 0) durationMs * 1000 else C.TIME_UNSET)
+      .build()
+
+    val commands = Player.Commands.Builder()
+      .add(Player.COMMAND_PLAY_PAUSE)
+      .add(Player.COMMAND_PREPARE)
+      .add(Player.COMMAND_SEEK_IN_CURRENT_MEDIA_ITEM)
+      .add(Player.COMMAND_GET_CURRENT_MEDIA_ITEM)
+      .add(Player.COMMAND_GET_METADATA)
+      // COMMAND_GET_TIMELINE is deliberately absent, and COMMAND_SEEK_TO_NEXT /
+      // PREVIOUS are added by the forwarding wrapper in AudioControlsService —
+      // see the notes on resolveSessionPlayer for both.
+      .build()
+
+    val state = State.Builder()
+      .setAvailableCommands(commands)
+      .setPlayWhenReady(playing, Player.PLAY_WHEN_READY_CHANGE_REASON_USER_REQUEST)
+      .setPlaybackState(Player.STATE_READY)
+      .setPlaylist(ImmutableList.of(item))
+      .setCurrentMediaItemIndex(0)
+      .setContentPositionMs(extrapolated.coerceAtLeast(0L))
+    // An explicit discontinuity always wins over media3's own heuristic, which
+    // would otherwise report this same-uid jump as INTERNAL — a reason the
+    // platform session does not act on. SimpleBasePlayer clears it after the
+    // update, so it applies to this state only.
+    if (pendingDiscontinuityMs != C.TIME_UNSET) {
+      state.setPositionDiscontinuity(
+        Player.DISCONTINUITY_REASON_SEEK,
+        pendingDiscontinuityMs
+      )
+    }
+    return state.build()
+  }
+
+  override fun handleSetPlayWhenReady(playWhenReady: Boolean): ListenableFuture<*> {
+    if (playWhenReady) onPlay() else onPause()
+    return Futures.immediateVoidFuture()
+  }
+
+  override fun handlePrepare(): ListenableFuture<*> = Futures.immediateVoidFuture()
+
+  override fun handleSeek(
+    mediaItemIndex: Int,
+    positionMs: Long,
+    seekCommand: Int
+  ): ListenableFuture<*> {
+    // Track skips never reach here: the forwarding wrapper intercepts
+    // seekToNext/Previous and routes them to the JS queue, so anything left is a
+    // position seek within the current track.
+    onSeek(positionMs.coerceAtLeast(0L))
+    return Futures.immediateVoidFuture()
+  }
+
+  private companion object {
+    const val REMOTE_ITEM_UID = "expo-audio-remote-playback"
+    const val POSITION_JUMP_TOLERANCE_MS = 1000L
+  }
+}
diff --git a/android/src/main/java/expo/modules/audio/service/AudioMediaSessionCallback.kt b/android/src/main/java/expo/modules/audio/service/AudioMediaSessionCallback.kt
index 839aaef12d7699ea1a89037b31849775daa079f4..9f5420f76b164ecdadab86a3f7f19a734702242f 100644
--- a/android/src/main/java/expo/modules/audio/service/AudioMediaSessionCallback.kt
+++ b/android/src/main/java/expo/modules/audio/service/AudioMediaSessionCallback.kt
@@ -10,7 +10,10 @@ import androidx.media3.common.util.UnstableApi
 import com.google.common.util.concurrent.ListenableFuture
 
 @OptIn(UnstableApi::class)
-class AudioMediaSessionCallback : MediaSession.Callback {
+class AudioMediaSessionCallback(
+  private val onSkipPrevious: () -> Unit = {},
+  private val onSkipNext: () -> Unit = {}
+) : MediaSession.Callback {
   override fun onConnect(
     session: MediaSession,
     controller: MediaSession.ControllerInfo
@@ -24,17 +27,21 @@ class AudioMediaSessionCallback : MediaSession.Callback {
             .add(Player.COMMAND_SEEK_IN_CURRENT_MEDIA_ITEM)
             .add(Player.COMMAND_SEEK_FORWARD)
             .add(Player.COMMAND_SEEK_BACK)
-            // Remove track navigation commands
-            .remove(Player.COMMAND_SEEK_TO_PREVIOUS_MEDIA_ITEM)
-            .remove(Player.COMMAND_SEEK_TO_NEXT_MEDIA_ITEM)
-            .remove(Player.COMMAND_SEEK_TO_PREVIOUS)
-            .remove(Player.COMMAND_SEEK_TO_NEXT)
+            // Advertise track navigation so hardware/Bluetooth media buttons
+            // (KEYCODE_MEDIA_NEXT/PREVIOUS) reach the session player, where the
+            // session player wrapper re-routes them to the JS-managed queue.
+            .add(Player.COMMAND_SEEK_TO_PREVIOUS_MEDIA_ITEM)
+            .add(Player.COMMAND_SEEK_TO_NEXT_MEDIA_ITEM)
+            .add(Player.COMMAND_SEEK_TO_PREVIOUS)
+            .add(Player.COMMAND_SEEK_TO_NEXT)
             .build()
         )
         .setAvailableSessionCommands(
           MediaSession.ConnectionResult.DEFAULT_SESSION_COMMANDS.buildUpon()
             .add(SessionCommand(AudioControlsService.ACTION_SEEK_BACKWARD, Bundle.EMPTY))
             .add(SessionCommand(AudioControlsService.ACTION_SEEK_FORWARD, Bundle.EMPTY))
+            .add(SessionCommand(AudioControlsService.ACTION_SKIP_PREVIOUS, Bundle.EMPTY))
+            .add(SessionCommand(AudioControlsService.ACTION_SKIP_NEXT, Bundle.EMPTY))
             .build()
         )
         .build()
@@ -56,6 +63,12 @@ class AudioMediaSessionCallback : MediaSession.Callback {
       AudioControlsService.ACTION_SEEK_BACKWARD -> {
         session.player.seekTo(session.player.currentPosition - AudioControlsService.SEEK_INTERVAL_MS)
       }
+      AudioControlsService.ACTION_SKIP_PREVIOUS -> {
+        onSkipPrevious()
+      }
+      AudioControlsService.ACTION_SKIP_NEXT -> {
+        onSkipNext()
+      }
     }
     return super.onCustomCommand(session, controller, command, args)
   }
diff --git a/android/src/main/java/expo/modules/audio/service/MetadataInjectingPlayer.kt b/android/src/main/java/expo/modules/audio/service/MetadataInjectingPlayer.kt
index 18203a364d17dcf3de95d22a20175308e3363889..0cbd9bb1ba694c92f02abdef6bef0db22d047161 100644
--- a/android/src/main/java/expo/modules/audio/service/MetadataInjectingPlayer.kt
+++ b/android/src/main/java/expo/modules/audio/service/MetadataInjectingPlayer.kt
@@ -45,6 +45,11 @@ internal class MetadataInjectingPlayer(
       .setAlbumTitle(metadata?.albumTitle)
       .setArtworkUri(metadata?.artworkUrl?.toString()?.toUri())
       .setArtworkData(null, null)
+      // Carry the known track duration on the metadata so the media notification's
+      // duration is authoritative. Media3 uses MediaMetadata.durationMs for
+      // METADATA_KEY_DURATION when the player's live content duration is unknown
+      // (e.g. right after a gapless transition, before the next item buffers).
+      .setDurationMs(metadata?.durationMs?.toLong())
       .build()
   }
 
diff --git a/build/Audio.types.d.ts b/build/Audio.types.d.ts
index 25604783649fe9b64f6655172f08471a5efcbf45..da73354dac3220b01cb9af6ab10e3ddc2fe05edf 100644
--- a/build/Audio.types.d.ts
+++ b/build/Audio.types.d.ts
@@ -618,6 +618,7 @@ export type AudioMetadata = {
     artist?: string;
     albumTitle?: string;
     artworkUrl?: string;
+    durationMs?: number;
 };
 /**
  * Loop mode for audio playlist playback.
diff --git a/build/AudioConstants.d.ts b/build/AudioConstants.d.ts
index cfd92ad0ff5765254c4e6db974fe44bce34aad06..daef7384bcd7090cc200dd65d3812fc6e5774306 100644
--- a/build/AudioConstants.d.ts
+++ b/build/AudioConstants.d.ts
@@ -15,5 +15,13 @@ export type AudioLockScreenOptions = {
      * and scrub bar, and disable seek controls.
      */
     isLiveStream?: boolean;
+    /**
+     * Whether the skip-to-previous-track button should be displayed on the lock screen.
+     */
+    showSkipPrevious?: boolean;
+    /**
+     * Whether the skip-to-next-track button should be displayed on the lock screen.
+     */
+    showSkipNext?: boolean;
 };
 //# sourceMappingURL=AudioConstants.d.ts.map
\ No newline at end of file
diff --git a/build/AudioModule.types.d.ts b/build/AudioModule.types.d.ts
index f3ae4ee4044d86032cd5c6dbcfee6f905d1ce99f..65155dffc72c3458e55e807330c62f58555f377b 100644
--- a/build/AudioModule.types.d.ts
+++ b/build/AudioModule.types.d.ts
@@ -92,6 +92,11 @@ export declare class AudioPlayer extends SharedObject<AudioEvents> {
      * ```
      */
     volume: number;
+    /**
+     * Android-only. The ExoPlayer audio session id used by this player, suitable for the
+     * `android.media.action.DISPLAY_AUDIO_EFFECT_CONTROL_PANEL` intent. `0` on other platforms.
+     */
+    readonly audioSessionId: number;
     /**
      * The current playback rate of the audio. It accepts different values depending on the platform:
      * - **Android**: `0.1` to `2.0`
@@ -178,6 +183,16 @@ export declare class AudioPlayer extends SharedObject<AudioEvents> {
      * @param metadata The metadata to display (title, artist, album, artwork).
      */
     updateLockScreenMetadata(metadata: AudioMetadata): void;
+    /**
+     * Android/iOS. Hand the OS media controls over to (or take them back from) a
+     * device other than this one — a Subsonic jukebox, a UPnP renderer.
+     */
+    setRemotePlayback(active: boolean): void;
+    /**
+     * Android/iOS. Push the remote device's transport state to the OS media
+     * controls. No-op unless `setRemotePlayback(true)` was called.
+     */
+    updateRemotePlayback(playing: boolean, positionMs: number, durationMs: number): void;
     /**
      * Removes this player from lock screen controls if it's currently active.
      * This will clear the lock screen's now playing info.
@@ -222,6 +237,16 @@ export type AudioEvents = {
     playbackStatusUpdate(status: AudioStatus): void;
     /** Fired when audio sampling is enabled and new sample data is available. */
     audioSampleUpdate(data: AudioSample): void;
+    /** Fired when the user taps the skip-previous-track button on the lock screen / notification / headset. */
+    remotePrevious(): void;
+    /** Fired when the user taps the skip-next-track button on the lock screen / notification / headset. */
+    remoteNext(): void;
+    /** Fired when the user presses play on the lock screen / notification while a remote device owns playback. */
+    remotePlay(): void;
+    /** Fired when the user presses pause on the lock screen / notification while a remote device owns playback. */
+    remotePause(): void;
+    /** Fired when the user scrubs the lock screen / notification seek bar while a remote device owns playback. */
+    remoteSeek(positionMs: number): void;
 };
 export declare class AudioRecorder extends SharedObject<RecordingEvents> {
     /**
diff --git a/build/AudioPlayer.web.d.ts b/build/AudioPlayer.web.d.ts
index 1a788415d3825e229822a539009425e6edede127..ea2ebbb2c4641f11a88a35eaa9dfcb6c47e76496 100644
--- a/build/AudioPlayer.web.d.ts
+++ b/build/AudioPlayer.web.d.ts
@@ -46,6 +46,8 @@ export declare class AudioPlayerWeb extends globalThis.expo.SharedObject<AudioEv
     setActiveForLockScreen(active: boolean, metadata?: AudioMetadata, options?: AudioLockScreenOptions): void;
     updateLockScreenMetadata(metadata: AudioMetadata): void;
     clearLockScreenControls(): void;
+    setRemotePlayback(_active: boolean): void;
+    updateRemotePlayback(_playing: boolean, _positionMs: number, _durationMs: number): void;
     _isCrossOrigin(): boolean;
     _createMediaElement(): HTMLAudioElement;
 }
diff --git a/build/AudioPlayer.web.js b/build/AudioPlayer.web.js
index 89bc87297090c5003d7ca2036e66b2dedf61e7b4..8f6ed9debaefc744d2eab771d91b24b101af7bd7 100644
--- a/build/AudioPlayer.web.js
+++ b/build/AudioPlayer.web.js
@@ -230,6 +230,8 @@ export class AudioPlayerWeb extends globalThis.expo.SharedObject {
     clearLockScreenControls() {
         mediaSessionController.clear(this);
     }
+    setRemotePlayback(_active) { }
+    updateRemotePlayback(_playing, _positionMs, _durationMs) { }
     _isCrossOrigin() {
         try {
             return new URL(this.media.src).origin !== window.location.origin;
diff --git a/ios/AudioModule.swift b/ios/AudioModule.swift
index d26560627c2dfa97370440ad28304617f3f9f0f7..931f8fcb197fab749a0cc694680592291f65e946 100644
--- a/ios/AudioModule.swift
+++ b/ios/AudioModule.swift
@@ -273,10 +273,23 @@ public class AudioModule: Module {
         if player.isActiveForLockScreen {
           player.metadata = nil
           player.isActiveForLockScreen = false
+          MediaController.shared.setRemotePlayback(false)
           MediaController.shared.setActivePlayer(nil)
         }
       }
 
+      Function("setRemotePlayback") { (_: AudioPlayer, active: Bool) in
+        MediaController.shared.setRemotePlayback(active)
+      }
+
+      Function("updateRemotePlayback") { (_: AudioPlayer, playing: Bool, positionMs: Double, durationMs: Double) in
+        MediaController.shared.updateRemotePlayback(
+          playing: playing,
+          position: positionMs / 1000,
+          duration: durationMs / 1000
+        )
+      }
+
       AsyncFunction("seekTo") { (player: AudioPlayer, seconds: Double, toleranceMillisBefore: Double?, toleranceMillisAfter: Double?) in
         await player.seekTo(
           seconds: seconds,
diff --git a/ios/AudioPlayer.swift b/ios/AudioPlayer.swift
index 074294321e872bb53c0b2d41044dac3c612f14f0..e84c1b5de2b98d41b5c22b4e43fbb69857d8febf 100644
--- a/ios/AudioPlayer.swift
+++ b/ios/AudioPlayer.swift
@@ -4,6 +4,35 @@ import Combine
 private enum AudioConstants {
   static let playbackStatus = "playbackStatusUpdate"
   static let audioSample = "audioSampleUpdate"
+  static let remotePrevious = "remotePrevious"
+  static let remoteNext = "remoteNext"
+  static let remotePlay = "remotePlay"
+  static let remotePause = "remotePause"
+  static let remoteSeek = "remoteSeek"
+}
+
+extension AudioPlayer {
+  func emitRemotePrevious() {
+    self.emit(event: AudioConstants.remotePrevious, payload: [])
+  }
+
+  func emitRemoteNext() {
+    self.emit(event: AudioConstants.remoteNext, payload: [])
+  }
+
+  // Only fired while a remote device owns playback; locally the media controls
+  // act on this player directly and never take the JS round trip.
+  func emitRemotePlay() {
+    self.emit(event: AudioConstants.remotePlay, payload: [])
+  }
+
+  func emitRemotePause() {
+    self.emit(event: AudioConstants.remotePause, payload: [])
+  }
+
+  func emitRemoteSeek(_ positionSeconds: Double) {
+    self.emit(event: AudioConstants.remoteSeek, payload: positionSeconds * 1000)
+  }
 }
 
 public class AudioPlayer: SharedRef<AVPlayer>, Playable {
diff --git a/ios/AudioRecords.swift b/ios/AudioRecords.swift
index 33db27c3b8fb40fa9db6659a0787c6b2b503d1ab..7999534bb8889646b5edc109f0846a5b36d201c5 100644
--- a/ios/AudioRecords.swift
+++ b/ios/AudioRecords.swift
@@ -70,6 +70,8 @@ struct LockScreenOptions: Record {
   @Field var showSeekForward: Bool = false
   @Field var showSeekBackward: Bool = false
   @Field var isLiveStream: Bool? = false
+  @Field var showSkipPrevious: Bool = false
+  @Field var showSkipNext: Bool = false
 }
 
 enum BitRateStrategy: String, Enumerable {
diff --git a/ios/MediaController.swift b/ios/MediaController.swift
index e977f577cd2e16b194b56537ad76b423f40d2496..bf8f13eb0ade6702399521ccf47b1f89dcbf821f 100644
--- a/ios/MediaController.swift
+++ b/ios/MediaController.swift
@@ -15,6 +15,46 @@ class MediaController {
   private var currentArtworkItemIdentifier: ObjectIdentifier?
   private var isLiveStream = false
 
+  // Playback that belongs to a remote device (a Subsonic jukebox, a UPnP
+  // renderer) rather than this player. While active, the now-playing info is
+  // fed from JS instead of the AVPlayer — which is holding nothing — and the
+  // transport commands are routed back to JS rather than acting locally.
+  private var remotePlaybackActive = false
+  private var remotePlaying = false
+  private var remotePosition: Double = 0
+  private var remoteDuration: Double = 0
+
+  func setRemotePlayback(_ active: Bool) {
+    performOnMain {
+      guard self.remotePlaybackActive != active else {
+        return
+      }
+      self.remotePlaybackActive = active
+      if !active {
+        self.remotePlaying = false
+        self.remotePosition = 0
+        self.remoteDuration = 0
+      }
+      if let player = self.activePlayer {
+        self.updateNowPlayingInfoOnMain(for: player)
+      }
+    }
+  }
+
+  func updateRemotePlayback(playing: Bool, position: Double, duration: Double) {
+    performOnMain {
+      guard self.remotePlaybackActive else {
+        return
+      }
+      self.remotePlaying = playing
+      self.remotePosition = max(0, position)
+      self.remoteDuration = max(0, duration)
+      if let player = self.activePlayer {
+        self.updateNowPlayingInfoOnMain(for: player)
+      }
+    }
+  }
+
   func setActivePlayer(_ player: AudioPlayer?, options: LockScreenOptions? = nil) {
     performOnMain {
       self.setActivePlayerOnMain(player, options: options)
@@ -75,6 +115,13 @@ class MediaController {
   }
 
   private func applyPlaybackInfo(_ info: inout [String: Any], for player: AudioPlayer) {
+    if remotePlaybackActive {
+      info[MPMediaItemPropertyPlaybackDuration] = remoteDuration
+      info[MPNowPlayingInfoPropertyElapsedPlaybackTime] = remotePosition
+      info[MPNowPlayingInfoPropertyPlaybackRate] = remotePlaying ? 1.0 : 0.0
+      info[MPNowPlayingInfoPropertyMediaType] = MPNowPlayingInfoMediaType.audio.rawValue
+      return
+    }
     if isLiveStream {
       info[MPNowPlayingInfoPropertyIsLiveStream] = true
       info.removeValue(forKey: MPMediaItemPropertyPlaybackDuration)
@@ -237,6 +284,10 @@ class MediaController {
         return .commandFailed
       }
 
+      if self?.remotePlaybackActive == true {
+        player.emitRemotePlay()
+        return .success
+      }
       player.play(at: Float(player.currentRate > 0 ? player.currentRate : 1.0))
       return .success
     }
@@ -246,6 +297,10 @@ class MediaController {
         return .commandFailed
       }
 
+      if self?.remotePlaybackActive == true {
+        player.emitRemotePause()
+        return .success
+      }
       player.ref.pause()
       return .success
     }
@@ -255,6 +310,14 @@ class MediaController {
         return .commandFailed
       }
 
+      if self?.remotePlaybackActive == true {
+        if self?.remotePlaying == true {
+          player.emitRemotePause()
+        } else {
+          player.emitRemotePlay()
+        }
+        return .success
+      }
       if player.isPlaying {
         player.ref.pause()
       } else {
@@ -269,6 +332,10 @@ class MediaController {
         return .commandFailed
       }
 
+      if self?.remotePlaybackActive == true {
+        player.emitRemoteSeek(event.positionTime)
+        return .success
+      }
       let seekTime = CMTime(seconds: event.positionTime, preferredTimescale: 1)
       player.ref.seek(to: seekTime)
 
@@ -303,12 +370,30 @@ class MediaController {
       return .success
     }
 
+    remoteCommandCenter.previousTrackCommand.addTarget { [weak self] _ in
+      guard let player = self?.activePlayer else {
+        return .commandFailed
+      }
+      player.emitRemotePrevious()
+      return .success
+    }
+
+    remoteCommandCenter.nextTrackCommand.addTarget { [weak self] _ in
+      guard let player = self?.activePlayer else {
+        return .commandFailed
+      }
+      player.emitRemoteNext()
+      return .success
+    }
+
     remoteCommandCenter.playCommand.isEnabled = true
     remoteCommandCenter.pauseCommand.isEnabled = true
     remoteCommandCenter.togglePlayPauseCommand.isEnabled = true
     remoteCommandCenter.changePlaybackPositionCommand.isEnabled = !isLiveStream
     remoteCommandCenter.skipForwardCommand.isEnabled = options?.showSeekForward ?? false
     remoteCommandCenter.skipBackwardCommand.isEnabled = options?.showSeekBackward ?? false
+    remoteCommandCenter.previousTrackCommand.isEnabled = options?.showSkipPrevious ?? false
+    remoteCommandCenter.nextTrackCommand.isEnabled = options?.showSkipNext ?? false
   }
 
   private func disableRemoteCommands() {
@@ -318,6 +403,8 @@ class MediaController {
     remoteCommandCenter.changePlaybackPositionCommand.isEnabled = false
     remoteCommandCenter.skipForwardCommand.isEnabled = false
     remoteCommandCenter.skipBackwardCommand.isEnabled = false
+    remoteCommandCenter.previousTrackCommand.isEnabled = false
+    remoteCommandCenter.nextTrackCommand.isEnabled = false
 
     // Remove event targets
     remoteCommandCenter.playCommand.removeTarget(self)
@@ -326,5 +413,7 @@ class MediaController {
     remoteCommandCenter.changePlaybackPositionCommand.removeTarget(self)
     remoteCommandCenter.skipForwardCommand.removeTarget(self)
     remoteCommandCenter.skipBackwardCommand.removeTarget(self)
+    remoteCommandCenter.previousTrackCommand.removeTarget(self)
+    remoteCommandCenter.nextTrackCommand.removeTarget(self)
   }
 }
diff --git a/src/Audio.types.ts b/src/Audio.types.ts
index 906753fa4fcab4323c391bc71c77e7c9f39b7379..860a2f0a010f32ecd75fb81a959a1d2afa0169cb 100644
--- a/src/Audio.types.ts
+++ b/src/Audio.types.ts
@@ -664,6 +664,11 @@ export type AudioMetadata = {
   artist?: string;
   albumTitle?: string;
   artworkUrl?: string;
+  /**
+   * Track duration in milliseconds. Used to set the media notification's
+   * duration when the player's live content duration isn't yet known.
+   */
+  durationMs?: number;
 };
 
 /**
diff --git a/src/AudioConstants.ts b/src/AudioConstants.ts
index dcc0452f7cae66b1bfaf77ed211416b092965bd9..779223ba2550809e621f4006c193beb29c7d15e5 100644
--- a/src/AudioConstants.ts
+++ b/src/AudioConstants.ts
@@ -16,4 +16,14 @@ export type AudioLockScreenOptions = {
    * and scrub bar, and disable seek controls.
    */
   isLiveStream?: boolean;
+  /**
+   * Whether the skip-to-previous-track button should be displayed on the lock screen.
+   * When pressed, the player emits a `remotePrevious` event for the JS layer to handle.
+   */
+  showSkipPrevious?: boolean;
+  /**
+   * Whether the skip-to-next-track button should be displayed on the lock screen.
+   * When pressed, the player emits a `remoteNext` event for the JS layer to handle.
+   */
+  showSkipNext?: boolean;
 };
diff --git a/src/AudioModule.types.ts b/src/AudioModule.types.ts
index 7e7537173d067fecef767a03dc0f1860c8aca7ac..033ebd2da8c90d62b91e94fcf843be706b782a31 100644
--- a/src/AudioModule.types.ts
+++ b/src/AudioModule.types.ts
@@ -127,6 +127,12 @@ export declare class AudioPlayer extends SharedObject<AudioEvents> {
    */
   volume: number;
 
+  /**
+   * Android-only. The ExoPlayer audio session id used by this player, suitable for the
+   * `android.media.action.DISPLAY_AUDIO_EFFECT_CONTROL_PANEL` intent. `0` on other platforms.
+   */
+  readonly audioSessionId: number;
+
   /**
    * The current playback rate of the audio. It accepts different values depending on the platform:
    * - **Android**: `0.1` to `2.0`
@@ -231,6 +237,19 @@ export declare class AudioPlayer extends SharedObject<AudioEvents> {
    * @param metadata The metadata to display (title, artist, album, artwork).
    */
   updateLockScreenMetadata(metadata: AudioMetadata): void;
+  /**
+   * Android/iOS. Hand the OS media controls over to (or take them back from) a
+   * device other than this one — a Subsonic jukebox, a UPnP renderer. While
+   * active the controls are fed by `updateRemotePlayback` and their transport
+   * buttons emit `remotePlay` / `remotePause` / `remoteSeek` instead of acting
+   * on this player.
+   */
+  setRemotePlayback(active: boolean): void;
+  /**
+   * Android/iOS. Push the remote device's transport state to the OS media
+   * controls. No-op unless `setRemotePlayback(true)` was called.
+   */
+  updateRemotePlayback(playing: boolean, positionMs: number, durationMs: number): void;
 
   /**
    * Removes this player from lock screen controls if it's currently active.
@@ -280,6 +299,16 @@ export type AudioEvents = {
   playbackStatusUpdate(status: AudioStatus): void;
   /** Fired when audio sampling is enabled and new sample data is available. */
   audioSampleUpdate(data: AudioSample): void;
+  /** Fired when the user taps the skip-previous-track button on the lock screen / notification / headset. */
+  remotePrevious(): void;
+  /** Fired when the user taps the skip-next-track button on the lock screen / notification / headset. */
+  remoteNext(): void;
+  /** Fired when the user presses play on the lock screen / notification while a remote device owns playback. */
+  remotePlay(): void;
+  /** Fired when the user presses pause on the lock screen / notification while a remote device owns playback. */
+  remotePause(): void;
+  /** Fired when the user scrubs the lock screen / notification seek bar while a remote device owns playback. */
+  remoteSeek(positionMs: number): void;
 };
 
 export declare class AudioRecorder extends SharedObject<RecordingEvents> {
diff --git a/src/AudioPlayer.web.ts b/src/AudioPlayer.web.ts
index 61db2df8bbb2723eca7415a10860bfdff791574e..c0c0b6d2d2dd29a041a707e3545dc36048dda606 100644
--- a/src/AudioPlayer.web.ts
+++ b/src/AudioPlayer.web.ts
@@ -312,6 +312,16 @@ export class AudioPlayerWeb
     mediaSessionController.clear(this);
   }
 
+  // Web has no remote-output concept of its own; the page's Media Session keeps
+  // reflecting this element either way.
+  setRemotePlayback(_active: boolean): void {}
+
+  updateRemotePlayback(
+    _playing: boolean,
+    _positionMs: number,
+    _durationMs: number
+  ): void {}
+
   _isCrossOrigin(): boolean {
     try {
       return new URL(this.media.src).origin !== window.location.origin;
