<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <uses-feature android:name="android.hardware.type.watch" />

  <!--
    No INTERNET permission on purpose. The watch is a remote control: it never
    streams, authenticates, downloads, or talks to a backend. Everything it
    renders arrives over the Data Layer from the phone.
  -->

  <application
    android:allowBackup="false"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:supportsRtl="true"
    android:theme="@android:style/Theme.DeviceDefault">

    <uses-library
      android:name="com.google.android.wearable"
      android:required="false" />

    <!--
      Companion, not standalone: without Wavio on the phone there is nothing to
      control, so Play should only offer this to users who have the phone app.
    -->
    <meta-data
      android:name="com.google.android.wearable.standalone"
      android:value="false" />

    <activity
      android:name="com.jmercier.wavio.wear.MainActivity"
      android:exported="true"
      android:label="@string/app_name"
      android:taskAffinity=""
      android:theme="@android:style/Theme.DeviceDefault">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
  </application>
</manifest>
