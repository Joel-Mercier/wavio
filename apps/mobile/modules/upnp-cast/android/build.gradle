apply plugin: 'com.android.library'
apply plugin: 'kotlin-android'

group = 'expo.modules.upnpcast'
version = '0.0.1'

def expoModulesCorePlugin = new File(project(":expo-modules-core").projectDir.absolutePath, "ExpoModulesCorePlugin.gradle")
apply from: expoModulesCorePlugin
applyKotlinExpoModulesCorePlugin()
useCoreDependencies()
useExpoPublishing()

android {
  namespace "expo.modules.upnpcast"
  compileSdk rootProject.ext.compileSdkVersion ?: 36
  defaultConfig {
    minSdkVersion rootProject.ext.minSdkVersion ?: 26
    targetSdkVersion rootProject.ext.targetSdkVersion ?: 36
  }
}

dependencies {
  // SSDP, SOAP and DIDL-Lite are all written against the published UPnP AV spec
  // here, so there is nothing to depend on but the Expo module runtime.
  implementation project(':expo-modules-core')
}
